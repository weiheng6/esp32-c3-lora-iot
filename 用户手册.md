# ESP32-C3 智能温湿度监控系统

## 产品简介

这是一款基于 ESP32-C3 的智能温湿度监控系统，专为物联网应用场景设计。设备集成了温湿度采集、继电器控制、WiFi 连接、MQTT 通信和 LoRa 远距离通信等核心功能，支持本地条件控制和定时控制，即使在无网络环境下也能独立运行。

---

## 产品优势

### 🎯 核心优势

| 优势 | 说明 |
|------|------|
| **无网独立运行** | 内置条件控制和定时控制（需结合RTC模块）逻辑，断网后仍可按预设策略自动运行 |
| **边缘端灵活配置** | 自带 AP 热点，无需电脑或手机 APP，通过手机或电脑浏览器访问即可完成全部配置 |
| **三重固件升级** | 支持 Web URL 升级、本地文件上传、MQTT 远程批量升级 |
| **智能省电设计** | WiFi 连接成功后自动关闭热点，AP 模式按需启用 |
| **工业级可靠性** | 内置故障检测和自动恢复机制，保障设备长期稳定运行 |
| **双协议支持** | MQTT 支持 JSON 和 Protobuf 两种协议，兼容各类物联网平台 |

### 🔧 功能特性

- 🌡️ **温湿度采集** - 高精度 SHT31 传感器，实时监测环境数据
- 🔌 **继电器控制** - 支持手动开关和自动控制模式
- 🎛️ **条件控制** - 基于温湿度的滞回控制，自动调节设备状态
- ⏰ **定时控制** - 最多 8 组时间段，灵活规划设备运行时间
- 📡 **MQTT 上报** - 数据实时上传至云端，支持远程监控和控制
- 📡 **LoRa 通信** - 远距离无线传输，适用于分布式传感器网络
- 🔄 **OTA 升级** - 支持远程固件更新，维护便捷

---

## 快速开始

### 设备连接

#### 方式一：AP 模式连接（推荐首次使用）

1. 设备上电后，等待约 10 秒，自动创建 WiFi 热点
2. 使用手机或电脑连接 WiFi：
   - **热点名称**：Scott_Device
   - **密码**：88888888
3. 连接成功后，打开浏览器访问：`http://192.168.4.1`
4. 进入 Web 配置界面进行参数设置

#### 方式二：局域网连接（已配置 WiFi）

1. 设备连接 WiFi 后，通过路由器查看设备 IP 地址
2. 或者从 MQTT 上报数据中获取设备 IP
3. 在同一局域网内的浏览器中访问该 IP 地址

### 首次配置

1. 进入 **📡 网络配置** 页面
2. 点击 WiFi SSID 旁边的 **🔍 扫描** 按钮
3. 选择要连接的 WiFi 网络，输入密码
4. 点击 **💾 保存WiFi** 按钮
5. 等待设备连接成功

### 配置 MQTT（可选）

1. 在 **📡 网络配置** 页面填写 MQTT 服务器信息
2. 设置服务器地址、端口、用户名、密码和 Topic
3. 点击 **💾 保存MQTT** 按钮

---

## 功能使用

### 📊 仪表盘

仪表盘是设备的主界面，实时显示：

- **温湿度数据** - 当前采集的温湿度数值
- **继电器状态** - 当前开关状态和运行模式
- **定时任务详情** - 已配置的时间段列表和执行状态
- **网络状态** - WiFi 和 MQTT 连接状态
- **快速控制** - 一键开关继电器

### 🎛️ 控制策略

#### 条件控制

条件控制是设备的核心功能之一，实现基于温湿度的自动控制：

1. 进入 **🎛️ 控制策略** 页面
2. 找到 **条件控制配置** 区域
3. 设置温度阈值：
   - **开启阈值**：例如 30°C（高于此温度继电器开启）
   - **关闭阈值**：例如 28°C（低于此温度继电器关闭）
4. 可选设置湿度阈值
5. 点击 **💾 保存配置** 按钮

**工作原理**：当温度高于 30°C 时继电器自动开启，低于 28°C 时自动关闭，形成滞回控制，避免频繁开关。

#### 定时控制

定时控制支持每天不同时段的自动控制：

1. 进入 **🎛️ 控制策略** 页面
2. 找到 **定时控制配置** 区域
3. 点击 **➕ 添加时间段** 按钮
4. 设置时间段：
   - 开始时间、结束时间
   - 执行动作（开启/关闭）
   - 启用/禁用该时间段
5. 重复上述步骤可添加最多 8 个时间段
6. 点击 **💾 保存配置** 按钮

**示例场景**：
- 08:00-12:00 开启设备
- 14:00-18:00 开启设备
- 其他时间关闭设备

### 🔧 设备配置

#### 引脚映射

查看设备的引脚配置和使用状态，便于后续调试和扩展：

| 功能 | GPIO | 说明 |
|------|------|------|
| 继电器控制 | GPIO20 | 控制继电器开关 |
| SHT31 数据 | GPIO8 | I2C 数据线 |
| SHT31 时钟 | GPIO9 | I2C 时钟线 |
| LoRa SCK | GPIO4 | SPI 时钟 |
| LoRa MISO | GPIO5 | SPI 输入 |
| LoRa MOSI | GPIO6 | SPI 输出 |
| LoRa NSS | GPIO7 | SPI 片选 |
| LoRa RST | GPIO10 | LoRa 复位 |

### 🔄 OTA 升级

设备支持三种固件升级方式：

#### Web URL 升级（推荐）

适用于远程升级场景：

1. 进入 **🔄 OTA升级** 页面
2. 选择 **URL升级** 选项卡
3. 输入固件下载地址（由厂商提供）
4. 点击 **开始升级** 按钮
5. 等待升级完成，设备自动重启

#### 本地文件上传

适用于没有网络连接的升级场景：

1. 进入 **🔄 OTA升级** 页面
2. 选择 **文件上传** 选项卡
3. 点击 **选择文件**，选择本地固件文件
4. 点击 **上传并升级** 按钮
5. 等待上传完成，设备自动升级

#### MQTT 远程升级

适用于批量设备统一升级：

发送升级指令到 MQTT 主题：`esp32/{设备ID}/command`

```json
{
  "ota": {
    "url": "https://your-server.com/firmware.bin"
  }
}
```

---

## 工作模式

### 本地独立运行模式

设备可在无网络环境下独立工作：

- ✅ 条件控制：根据温湿度自动控制继电器
- ✅ 定时控制：按设定时间段自动控制（需结合RTC模块）
- ✅ 本地存储：配置参数持久化保存
- ✅ 手动控制：Web 界面直接操作

### 云端联网模式

设备连接网络后增强功能：

- ✅ MQTT 数据上报：实时推送温湿度数据
- ✅ 远程控制：通过 MQTT 远程开关继电器
- ✅ 远程配置：MQTT 修改设备参数
- ✅ 远程升级：MQTT 远程推送固件更新

### 混合模式

设备可同时运行本地控制和云端上报：

1. 本地条件控制和定时控制持续运行
2. 温湿度数据定期上传至云端
3. 云端可实时查看数据但不能覆盖本地控制逻辑
4. 适用于需要本地可靠性的场景

---

## 技术规格

### 硬件参数

| 参数 | 规格 |
|------|------|
| 主控芯片 | ESP32-C3 |
| 无线频率 | 2.4GHz WiFi + LoRa 433MHz |
| 工作电压 | 5V DC |
| 工作温度 | -40°C ~ 85°C |
| 温湿度精度 | ±0.3°C / ±2%RH |

### 软件功能

| 功能 | 支持情况 |
|------|----------|
| 条件控制 | ✅ 支持滞回控制 |
| 定时控制 | ✅ 最多 8 组时间段 |
| MQTT 协议 | ✅ JSON + Protobuf |
| OTA 升级 | ✅ 三种方式 |
| LoRa 通信 | ✅ 可选模块 |
| 故障恢复 | ✅ 自动重启保护 |

---

## 故障排查

### 常见问题

#### Q1：设备热点搜不到？

**解决方案**：
- 等待设备上电 30 秒后再搜索
- 确认设备正常供电（指示灯亮起）
- 尝试重启设备

#### Q2：WiFi 连接后无法访问 Web 界面？

**解决方案**：
- 确认手机/电脑已连接到设备热点
- 检查浏览器地址栏是否输入正确：`http://192.168.4.1`
- 尝试清除浏览器缓存后重新访问

#### Q3：设备频繁重启？

**解决方案**：
- 检查电源供电是否稳定（建议 5V/1A）
- 查看串口日志判断具体原因
- 联系技术支持获取帮助

#### Q4：MQTT 连接失败？

**解决方案**：
- 确认 MQTT 服务器地址和端口正确
- 检查用户名和密码是否匹配
- 确认网络可以访问 MQTT 服务器

#### Q5：条件控制不生效？

**解决方案**：
- 确认条件控制已启用
- 检查阈值设置是否合理
- 确认继电器在自动模式下

#### Q6：定时任务时间不准？

**解决方案**：
- 检查设备时间是否通过网络同步
- 确认时间段设置正确
- 查看仪表盘中的"当前时间"显示


## 安全须知

### 使用环境

- 请在干燥、通风的环境中使用
- 避免在极端温度或潮湿环境中长时间运行
- 继电器输出负载不要超过额定功率

### 网络安全

- 首次使用请及时修改默认密码
- 建议使用 WPA2-PSK 加密的 WiFi 网络
- MQTT 通信建议使用 TLS/SSL 加密

### 固件更新

- 仅使用官方提供的固件进行升级
- 升级过程中不要断电
- 升级完成后验证设备功能正常

---

## 维护保养

### 定期检查

- 每月检查设备运行环境
- 每季度检查固件版本，及时升级
- 定期检查继电器触点状态

### 数据备份

- 重要配置可在 Web 界面手动记录
- 设备支持配置导出（如有该功能）

### 固件更新

- 关注厂商发布的固件更新公告
- 更新前阅读更新说明
- 大版本更新建议在维护时间段进行

---


### 获取帮助信息

联系技术支持时，请提供以下信息：

1. 设备型号和固件版本（可在系统信息页面查看）
2. 问题描述和复现步骤
3. 串口日志输出（如有）
4. 网络环境说明

---

## 版本信息

当前固件版本：v1.5.0

更新日期：2026-05-12

**本版本更新内容**：
- 新增 WiFi 扫描功能
- 优化智能 AP 模式管理
- 修复定时控制多项 BUG
- 完善配置同步机制

---

*感谢您选择我们的产品，祝您使用愉快！*
