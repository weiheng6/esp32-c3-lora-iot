# 🚀 快速开始指南 - P0 功能使用

## 一、固件上传

### 方式 1：USB 连接（首次上传）

```powershell
cd f:\arduinoWorkspace\lora\platform-esp32-c3

# 构建固件
pio run -e adafruit_qtpy_esp32c3

# 上传到设备（替换 COM9 为实际端口）
pio run -e adafruit_qtpy_esp32c3 -t upload --upload-port COM9

# 查看串口日志
pio device monitor -b 115200 -p COM9
```

---

## 二、通过 Web UI 配置

### 第 1 步：连接到设备热点

1. 打开 WiFi 列表，找到 `Scott_Device`
2. 输入密码：`88888888`
3. 连接成功

### 第 2 步：打开 Web 界面

在浏览器中访问：
```
http://192.168.4.1
```

你会看到一个现代化的配置界面！

### 第 3 步：配置参数

#### 📊 仪表板 (Dashboard)
- 实时查看 WiFi、MQTT、传感器状态
- 内存使用率监控
- 继电器开关状态

#### 📡 MQTT 配置
输入你的 MQTT 服务器信息：
```
服务器地址：iot.kebaidata.com
端口：1883
用户名：kbuser
密码：kbuserspasswd
```

#### ⏱️ 采集配置
设置数据采集和上报频率：
```
传感器采集间隔：1-3600 秒（每隔多久读一次温湿度）
MQTT 上报间隔：1-3600 秒（每隔多久发送一次数据到 MQTT）
```

#### 🔌 继电器控制
- ✅ **开启** - 打开继电器
- ❌ **关闭** - 关闭继电器
- 自动模式 - 根据温湿度自动控制

#### ⚙️ 系统设置
```
💾 保存配置     - 保存所有修改
📥 导出配置     - 备份当前配置为 JSON 文件
📤 导入配置     - 从 JSON 文件恢复配置
🔌 重启设备     - 重启设备应用新配置
```

---

## 三、OTA 无线升级（Arduino IDE）

### 前置要求
- 设备已连接到 WiFi 网络
- Arduino IDE 已安装 ESP32 核心库

### 步骤

#### 1️⃣ 新建或打开项目
在 Arduino IDE 中打开你的项目

#### 2️⃣ 选择网络端口
```
工具 → 端口 → 选择 esp32-c3-XXXXXX (IP地址)
```

示例：`esp32-c3-8856A6 (192.168.1.100)`

#### 3️⃣ 上传固件
```
草图 → 上传 (Ctrl+U)
```

#### 4️⃣ 观察进度
```
🔄 [OTA] 开始固件升级...
🔄 [OTA] 升级进度: 100/890 (11%)
🔄 [OTA] 升级进度: 445/890 (50%)
🔄 [OTA] 升级进度: 890/890 (100%)
✅ [OTA] 固件升级完成！
🔄 [OTA] 设备重启中...
```

#### 5️⃣ 完成！
设备自动重启，新固件生效

---

## 四、故障恢复系统

系统会自动监测和恢复以下故障：

### 🔴 I2C 总线故障
```
触发条件：传感器读取失败 > 50 次
自动动作：复位 I2C 总线并继续工作
日志输出：
  ⚠️ [ErrorRecovery] I2C 故障次数: 50
  🔄 [ErrorRecovery] I2C 总线故障，执行总线复位...
  ✅ [ErrorRecovery] I2C 总线已复位
```

### 🔴 内存不足
```
触发条件：剩余内存 < 20KB
自动动作：重启设备进行内存清理
日志输出：
  ❌ [ErrorRecovery] 内存严重不足: 18000 字节
  🔄 [ErrorRecovery] 执行内存恢复重启...
```

### 🔴 MQTT 连接问题
```
触发条件：连接失败 > 100 次
自动动作：强制重新连接
日志输出：
  ⚠️ [ErrorRecovery] MQTT 故障次数: 100
  🔄 [ErrorRecovery] MQTT 故障过多，执行重新连接...
```

### 启动次数追踪
```
如果设备启动超过 10 次，会打印警告：
  ⚠️⚠️ [ErrorRecovery] 检测到可能的重启循环！
  原因可能：
    1. 固件 bug
    2. 传感器硬件故障
    3. 内存不足
  建议：检查串口日志，考虑重新编译固件
```

---

## 五、配置导出/导入

### 导出配置（备份）

1. 打开 Web UI
2. 进入 **⚙️ 系统设置** 标签页
3. 点击 **📥 导出配置**
4. 浏览器自动下载 `device_config_TIMESTAMP.json`

### 导入配置（恢复）

1. 打开 Web UI
2. 进入 **⚙️ 系统设置** 标签页
3. 点击 **📤 导入配置**
4. 选择之前导出的 JSON 文件
5. 点击确认导入
6. 重启设备应用新配置

### 配置文件格式

```json
{
  "mqtt_server": "iot.kebaidata.com",
  "mqtt_port": 1883,
  "mqtt_user": "kbuser",
  "mqtt_password": "kbuserspasswd",
  "acquisition_interval": 1,
  "report_interval": 1,
  "temp_threshold": 25.0,
  "hum_threshold": 60.0,
  "condition_enabled": true,
  "timestamp": 1234567890
}
```

---

## 六、串口调试日志

### 连接串口监视

```powershell
pio device monitor -b 115200
```

### 典型启动日志

```
🔄 初始化传感器...
✅ 传感器初始化成功！

🔄 初始化 WiFi...
WiFi 已连接 (SSID: YOUR_SSID)

🔄 连接 MQTT...
✅ MQTT 已连接

🔄 初始化模块...
✅ 所有模块初始化完成！
════════════════════════════════════════

📊 [ErrorRecovery] 设备启动次数: 5

✅ OTA 已启动 - 主机名: esp32-c3-8856A6
(可通过 Arduino IDE 上传固件)

✅ Web UI 已启动 (http://192.168.4.1)

🎉 固件初始化完成！
```

### 实时数据上报日志

```
📡 [MQTT] 发送数据: {"temp":23.45,"hum":58.23,"relay":0}
✅ [MQTT] 发送成功

🔄 [ErrorRecovery] 诊断统计:
   启动次数: 5
   当前会话 I2C 故障: 0
   当前会话 MQTT 故障: 0
   剩余内存: 281996 字节 (14% 使用率)
```

---

## 七、常见问题

### Q: Web UI 打不开？
**A:** 检查以下步骤：
1. 确认已连接到 `Scott_Device` WiFi
2. 确认 IP 地址是 `192.168.4.1`
3. 尝试在浏览器中清除缓存
4. 查看串口日志中是否有错误

### Q: 配置保存后没有效果？
**A:** 需要重启设备：
1. 在 Web UI 中点击 **🔌 重启设备**
2. 或在串口发送 `RESTART` 命令
3. 等待设备重启完成

### Q: OTA 升级失败？
**A:** 检查以下条件：
1. 设备已连接到 WiFi（不是热点模式）
2. Arduino IDE 正确识别了网络端口
3. 固件大小不超过 Flash 大小
4. 查看串口日志中的错误信息

### Q: 设备频繁重启？
**A:** 这可能表示：
1. 传感器硬件故障（查看 I2C 故障计数）
2. 内存不足（查看剩余内存）
3. 固件有 bug（查看串口日志）

---

## 八、性能指标

```
硬件配置：
  芯片：ESP32-C3 (160 MHz)
  内存：320 KB RAM, 4 MB Flash
  传感器：Adafruit SHT31 (I2C)
  通信：WiFi, MQTT, LoRa

固件大小：
  Flash 占用：890 KB (67.9%)
  RAM 占用：45 KB (13.9%)
  可用空间：还有 420 KB Flash 用于未来扩展

性能：
  循环周期：平均 20 ms
  WiFi 连接时间：~3 秒
  MQTT 连接时间：~2 秒
  传感器读取时间：~20 ms
  Web UI 响应时间：<1 秒
```

---

## 九、技术支持

### 获取帮助

1. **查看日志** - 串口监视中的详细日志
2. **检查错误代码** - `[ERROR]` 开头的信息
3. **导出诊断信息** - 通过 Web UI 导出配置

### 联系方式

遇到问题时，请收集：
- 串口日志（完整的启动到故障过程）
- 导出的配置文件
- 硬件信息（传感器型号、WiFi 信号强度等）

---

**祝你使用愉快！** 🎉

有任何问题或建议，欢迎反馈！
