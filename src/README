# 源代码目录说明

这里放的是所有项目的"实现"文件（`.cpp` 文件）。

## 文件说明

| 文件名 | 作用 |
|--------|------|
| **main.cpp** | 主程序入口，启动和运行整个设备 |
| **sensor.cpp** | 温湿度传感器的实现 |
| **wifi_manager.cpp** | WiFi 连接的实现 |
| **mqtt_manager.cpp** | MQTT 云通讯的实现 |
| **relay_control.cpp** | 继电器控制的实现 |
| **condition_control.cpp** | 自动判断规则的实现 |
| **web_server.cpp** | 网页配置的实现 |
| **lora_manager.cpp** | LoRa 远距离通讯的实现 |
| **system_monitor.cpp** | 系统监控的实现 |

## 简单理解

`.cpp` 文件就像 **LEGO 积木本身**：
- 真正做事的代码
- 对应 `include/` 里的说明书
- 一般不需要你改

## 什么时候改这里？

**只有在**：
1. 修改传感器读取逻辑
2. 改 WiFi 连接方式
3. 改 MQTT 上报间隔
4. 改自动控制的判断逻辑

**大多数时候不需要改。** 直接改 `config.h` 就够了！
