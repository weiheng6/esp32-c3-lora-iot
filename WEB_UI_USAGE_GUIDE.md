# 🌐 Web UI 配置使用完整指南

## 工作流程概览

```
┌─────────────────────────────────────────────────────────────┐
│ 设备首次启动 → 无 WiFi 配置 → AP 热点模式                  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 用户步骤 1: 连接 AP 热点                                   │
│ • WiFi: Scott_Device                                       │
│ • 密码: 88888888                                          │
│ • 访问: http://192.168.4.1                               │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 用户步骤 2: WiFi 配置页面（在 AP 热点中）                  │
│ • 扫描可用的 WiFi 网络                                    │
│ • 输入 WiFi 密码                                         │
│ • 点击保存                                               │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 设备自动连接 WiFi 网络                                    │
│ • 显示局域网 IP 地址（如 192.168.x.x）                    │
│ • 显示 Web UI 访问地址                                   │
│ • 倒计时 30 秒后重启                                     │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 用户步骤 3: 断开 AP 热点，连接到同一个 WiFi 网络          │
│ • WiFi 名称（SSID）                                       │
│ • WiFi 密码（前面配置时输入的）                          │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 用户步骤 4: 访问 Web UI 管理界面                          │
│ • 在浏览器中输入: http://设备IP地址                      │
│ • 例如: http://192.168.1.100                            │
│ • 进行高级配置（MQTT、采集等）                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 📱 第 1 步：初始 WiFi 配置（通过 AP 热点）

### 前提条件
- 设备刚刷入固件或已重置 WiFi 配置
- 查看串口日志，出现类似：
  ```
  🔄 WiFi 未配置，启动 AP 模式...
  🔄 启动 AP 模式...
  📶 AP 已启动，IP 地址：192.168.4.1
  📱 请连接 WiFi：Scott_Device
  🔑 密码：88888888
  🌐 然后在浏览器中访问：http://192.168.4.1
  ```

### 操作步骤

#### 1️⃣ 在手机/平板上连接 AP 热点

**在手机的 WiFi 设置中：**
```
找到并点击：Scott_Device
输入密码：88888888
等待连接成功
```

#### 2️⃣ 打开浏览器访问配置页面

```
在浏览器地址栏输入：http://192.168.4.1
```

你会看到一个 WiFi 配置页面，包含：
- **WiFi 名称输入框** - 输入你要连接的 WiFi 名称（SSID）
- **WiFi 密码输入框** - 输入对应的密码
- **扫描 WiFi 按钮** - 可选，自动扫描周围的 WiFi 网络

#### 3️⃣ 配置 WiFi 信息

**方式 A：手动输入（推荐）**
```
1. 点击 "WiFi 名称" 输入框
2. 输入你的 WiFi 网络名称（SSID）
3. 点击 "WiFi 密码" 输入框
4. 输入你的 WiFi 密码
5. 点击 "保存配置" 按钮
```

**方式 B：扫描 WiFi（更方便）**
```
1. 点击 "扫描 WiFi" 按钮
2. 等待扫描完成（通常 2-3 秒）
3. 在列表中找到你的 WiFi 网络
4. 点击网络名称（自动填充）
5. 输入密码
6. 点击 "保存配置" 按钮
```

---

## 📡 第 2 步：确认 WiFi 连接（获取设备 IP）

### 页面显示（关键！）

配置完成后，会显示一个成功页面，其中包含：

```
✅ WiFi 配置成功！

已连接的 WiFi 网络
[你的 WiFi SSID]

设备内网 IP 地址
192.168.x.x（例如：192.168.1.100）

🌐 访问管理界面
[点击按钮或复制地址]

⏱️ 设备将在 30 秒后重启
```

**重要：记下这个 IP 地址！** 例如：`192.168.1.100`

### 等待重启

页面会显示 30 秒倒计时，设备将自动重启。在重启期间：
- ✅ 可以关闭这个页面
- ✅ 可以继续看倒计时
- ❌ 不要切掉电源

---

## 📶 第 3 步：连接到同一个 WiFi 网络

设备重启后，AP 热点会关闭，你的手机/电脑需要连接到同一个 WiFi 网络。

### 操作步骤

#### 手机连接方式
```
1. 打开手机 WiFi 设置
2. 找到你配置的 WiFi 网络（SSID）
3. 输入密码
4. 连接成功
```

#### 电脑连接方式
```
1. 打开电脑 WiFi 设置
2. 找到你配置的 WiFi 网络（SSID）
3. 输入密码
4. 连接成功
```

---

## 🌐 第 4 步：访问 Web UI 管理界面

现在你的手机/电脑和设备在同一个 WiFi 网络内，可以访问 Web UI。

### 获取设备 IP 地址

你需要知道设备在局域网中的 IP 地址。有几种方式获取：

#### 方式 1：从配置成功页面（推荐）
前面的配置成功页面显示的 IP 地址就是！
```
例如：192.168.1.100
```

#### 方式 2：查看路由器
登录你的 WiFi 路由器后台管理页面，查看连接的设备列表，找到 `esp32-c3` 或 `Scott_Device` 相关的设备。

#### 方式 3：从串口日志
连接串口监视，查看启动日志中的 IP 地址：
```
📶 WiFi 已连接，IP 地址：192.168.1.100
✅ Web UI 已启动（WiFi 连接后）- 访问地址：http://192.168.1.100
```

### 打开 Web UI

在浏览器地址栏输入设备的 IP 地址：

```
http://192.168.1.100
```

（把 `192.168.1.100` 替换成你实际的设备 IP 地址）

你会看到一个现代化的管理界面，包含：
- 📊 **仪表板** - 实时状态（WiFi、MQTT、温度、湿度、内存）
- 📡 **MQTT 设置** - 服务器地址、端口、用户名密码
- ⏱️ **采集设置** - 采集间隔、上报间隔
- 🔌 **继电器控制** - 开关按钮
- 🎯 **条件控制** - 自动温湿度控制
- ⚙️ **系统设置** - 保存、导出、导入、重启

---

## 🔧 Web UI 功能详解

### 📊 仪表板（Dashboard）

实时显示设备的当前状态：
```
WiFi 连接：✅ 已连接 / ❌ 未连接
MQTT 连接：✅ 已连接 / ❌ 未连接
温度：23.45 °C
湿度：58.23 %
继电器：🔴 开启 / ⚫ 关闭
内存使用：45.2 %

[🔄 刷新状态] 按钮
```

### 📡 MQTT 配置

配置设备连接的 MQTT 服务器：
```
MQTT 服务器地址：iot.kebaidata.com
MQTT 端口：1883
MQTT 用户名：kbuser
MQTT 密码：（password input）
```

修改后需要点击 **💾 保存所有配置** 并重启设备才能生效。

### ⏱️ 采集配置

配置传感器读取和数据上报的频率：
```
传感器采集间隔（秒）：1-3600
  → 每隔多久读取一次温湿度传感器

MQTT 上报间隔（秒）：1-3600
  → 每隔多久把数据发送到 MQTT 服务器
```

**例子**：
- 采集间隔 1 秒，上报间隔 10 秒
- = 每 1 秒读取传感器，每 10 秒发送一次数据

### 🔌 继电器控制

直接控制继电器的开关：
```
[✅ 开启]  [❌ 关闭]

状态：开启 / 关闭
```

### 🎯 条件控制

启用自动温湿度控制（可选）：
```
☑️ 启用自动温湿度控制

温度阈值（°C）：25.0
  → 当温度超过此值时自动开启继电器

湿度阈值（%）：60.0
  → 当湿度超过此值时自动开启继电器
```

### ⚙️ 系统设置

```
[💾 保存所有配置]  - 保存对配置的修改
[📥 导出配置]      - 下载配置文件（JSON 格式）
[📤 导入配置]      - 从 JSON 文件恢复配置
[🔌 重启设备]      - 立即重启设备
```

---

## 🔄 WiFi 重置（回到配置模式）

如果需要重新配置 WiFi 或改变连接的网络：

### 方式 1：通过 MQTT 命令（如果已配置）
```
发送 MQTT 消息到 esp32/{设备ID}/command
消息：{"reset_wifi": true}

设备会重置 WiFi 配置并重启，回到 AP 热点模式
```

### 方式 2：长按重置按钮（如果硬件支持）
查看你的设备文档了解重置按钮位置。

### 方式 3：通过 Web UI（不需要命令）
（当前版本不支持，可以在后续添加）

### 重置后的操作
```
1. 设备重启，进入 AP 热点模式
2. 连接 Scott_Device 热点
3. 访问 http://192.168.4.1
4. 重新配置 WiFi
5. 完成！
```

---

## 🐛 常见问题

### Q1：为什么配置成功页面显示的 IP 无法访问？

**原因**：可能有以下几种情况
1. IP 地址输入错误（复制粘贴可能有空格）
2. WiFi 网络不稳定，设备未成功连接
3. 防火墙阻止了访问
4. 设备和手机不在同一个子网

**解决方案**：
```
1. 检查设备的串口日志，确认 WiFi 已连接
2. 查看路由器的 DHCP 客户端列表，找到设备的真实 IP
3. 尝试 ping 设备的 IP 地址（命令行）
   ping 192.168.1.100
4. 如果 ping 通但网页打不开，可能是防火墙问题
5. 如果都不行，回到步骤 1 重新配置
```

### Q2：连接 AP 热点后打不开 http://192.168.4.1

**原因**：
1. WiFi 连接不稳定
2. 浏览器缓存问题
3. 设备尚未进入 AP 模式

**解决方案**：
```
1. 确保 WiFi 已连接（查看信号强度）
2. 尝试其他浏览器或私密模式
3. 查看串口日志，确认设备已启动 AP 模式
4. 重启设备（断电 10 秒重新上电）
```

### Q3：配置成功后设备频繁重启

**原因**：
1. WiFi 密码错误，连接失败
2. WiFi 网络不稳定
3. 设备内存不足

**解决方案**：
```
1. 检查 WiFi 密码是否正确
   • 区分大小写
   • 特殊字符可能被转义
2. 尝试靠近路由器
3. 查看串口日志了解具体错误信息
4. 重置 WiFi 后重新配置
```

### Q4：Web UI 在 WiFi 连接后无法访问

**原因**：
1. webUIManager 还未启动
2. IP 地址变化
3. WiFi 连接断开后未自动重连

**解决方案**：
```
1. 等待 10 秒（给 webUIManager 启动时间）
2. 刷新页面或重新输入地址
3. 查看串口日志：
   ✅ Web UI 已启动（WiFi 连接后）- 访问地址：http://192.168.x.x
4. 如果日志中没有，手动重启设备
```

### Q5：能连接 AP 热点但无法扫描到 WiFi 网络

**原因**：
1. 设备信号弱
2. WiFi 驱动程序问题
3. 周围 WiFi 信号过强（干扰）

**解决方案**：
```
1. 手动输入 WiFi 名称和密码（不要用扫描功能）
2. 靠近路由器
3. 重启设备
4. 如果还是不行，可能需要固件更新
```

---

## 📊 工作原理示意

```
首次使用：
┌─ 设备启动
├─ 检查 WiFi 配置
├─ 未配置 → 启动 AP 热点（Scott_Device）
├─ 用户连接 AP 热点
├─ 访问 http://192.168.4.1
├─ 填写 WiFi 信息并保存
├─ 设备立即尝试连接 WiFi
├─ 连接成功 → 显示局域网 IP
├─ 用户看到 Web UI 访问地址
├─ 设备在 30 秒后重启
└─ AP 热点关闭，进入正常工作模式

日常使用：
┌─ 设备启动
├─ 自动连接已配置的 WiFi
├─ 获得局域网 IP（如 192.168.1.100）
├─ 启动 Web UI
├─ Web UI 监听在 http://192.168.1.100
├─ OTA 固件升级服务启动
├─ MQTT 连接
└─ 正常工作，接收 MQTT 命令
```

---

## 总结

| 阶段 | 热点 | Web 页面 | 设备状态 |
|------|------|---------|--------|
| **配置阶段** | Scott_Device | http://192.168.4.1 | AP 模式，等待配置 |
| **配置中** | Scott_Device | 正在配置... | 尝试连接 WiFi |
| **配置完成** | Scott_Device | 显示 IP 地址和访问链接 | WiFi 已连接 |
| **重启后** | ❌ 关闭 | http://192.168.x.x | WiFi 已连接，Web UI 运行 |
| **日常使用** | ❌ 无 | http://192.168.x.x | 正常工作 |

**关键点**：
- 🔴 **配置必须在 AP 热点中完成**（192.168.4.1）
- 🟢 **使用必须在同一 WiFi 网络中**（192.168.x.x）
- 🔵 **两个阶段使用不同的网络和 IP 地址**

祝你使用愉快！如有问题，查看串口日志获取详细错误信息。
