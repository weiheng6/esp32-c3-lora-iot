# 🚀 Web UI 快速参考卡

## 三个 IP 地址，三个用途

```
设备启动时 → 检查 WiFi 配置
            ↓
        有配置？
        ↙      ↘
      是        否
      ↓        ↓
   WiFi   ┌─ AP 热点启动
   连接   │  WiFi: Scott_Device
   ↓     │  密码: 88888888
 获得IP  │  IP: 192.168.4.1 ✅ 用于初始配置
   ↓     │
 启动    └─ 用户访问 http://192.168.4.1
Web UI   │  填写 WiFi 信息
   ↓     │  保存并连接
IP地址  │
(如      │
192.168  ├─ 设备自动重启
1.100)   │
   ↓     │
✅用于   └─ AP 热点关闭
日常    
管理    ┌─ 用户连接到同一个 WiFi
       │  获得新 IP 地址
       │  例如：192.168.1.100 ✅ 用于日常管理
       │
       └─ 访问 http://192.168.1.100
          进行高级配置和管理
```

---

## 📱 第一次使用（5 分钟快速指南）

### 步骤 1：连接 AP 热点（30 秒）
```
WiFi 名称：Scott_Device
密码：88888888
```

### 步骤 2：配置 WiFi（2 分钟）
```
打开浏览器：http://192.168.4.1
输入你的 WiFi 名称和密码
点击 "保存配置"
等待显示设备 IP 地址
```

### 步骤 3：获取设备地址（自动完成）
```
配置成功页面会显示：
设备内网 IP 地址：192.168.1.100（示例）

把这个地址保存下来 ✅
```

### 步骤 4：等待重启（30 秒）
```
倒计时显示设备即将重启
设备重启后 AP 热点会关闭
不要关闭网络连接
```

### 步骤 5：断开 AP，连接 WiFi（30 秒）
```
手机：断开 Scott_Device 热点
手机：连接到你配置的 WiFi 网络
输入密码
等待连接成功
```

### 步骤 6：访问 Web UI（即时）
```
打开浏览器
输入设备的 IP 地址：http://192.168.1.100
看到管理界面 ✅
```

---

## 🎯 关键 IP 地址速查表

| 用途 | IP 地址 | 访问方式 | 何时使用 |
|------|--------|--------|--------|
| **WiFi 初始配置** | 192.168.4.1 | 连接 Scott_Device 热点后访问 | 首次配置或重置 WiFi |
| **日常管理** | 192.168.x.x（动态） | 连接到同一 WiFi 后访问 | 日常使用 |

---

## 💾 三个关键操作

### 操作 1：配置 WiFi（首次）
```
1. 连接 Scott_Device 热点
2. 访问 http://192.168.4.1
3. 输入 WiFi 名称和密码
4. 点击保存
5. ✅ 完成，设备会自动重启
```

### 操作 2：访问 Web UI（日常）
```
1. 确保手机/电脑和设备在同一 WiFi
2. 在浏览器中输入设备 IP
   例如：http://192.168.1.100
3. ✅ 进入管理界面
```

### 操作 3：重新配置（如需修改 WiFi）
```
1. 需要硬件重置按钮或 MQTT 命令
2. 重置 WiFi 配置
3. 设备重启进入 AP 模式
4. 回到操作 1
```

---

## 🔍 故障排查速查

| 问题 | 症状 | 解决方案 |
|------|------|--------|
| 打不开 192.168.4.1 | 配置页面无法加载 | 1. 重新连接 WiFi<br>2. 查看串口日志<br>3. 重启设备 |
| WiFi 配置失败 | 显示连接失败 | 1. 检查密码是否正确<br>2. 尝试手动输入 WiFi 名称<br>3. 靠近路由器 |
| 无法访问设备 IP | 配置完成但网页打不开 | 1. 确认手机已连接到同一 WiFi<br>2. 确认 IP 地址正确<br>3. 等待 10 秒给 Web UI 启动时间 |
| 频繁重启 | 设备不停重启 | 1. 检查 WiFi 密码<br>2. 查看串口日志找原因<br>3. 考虑硬件问题 |

---

## 📡 串口日志参考

### 正常启动日志（AP 模式）
```
🔄 WiFi 未配置，启动 AP 模式...
📶 AP 已启动，IP 地址：192.168.4.1
📱 请连接 WiFi：Scott_Device
🔑 密码：88888888
🌐 然后在浏览器中访问：http://192.168.4.1
```

### 配置成功日志（WiFi 已连接）
```
🔄 正在连接 WiFi：MyWiFi...✅
📶 WiFi 已连接，IP 地址：192.168.1.100
✅ Web UI 已启动（WiFi 连接后）- 访问地址：http://192.168.1.100
```

### 异常日志（帮助诊断）
```
❌ 连接失败            → WiFi 密码错误或网络不稳定
⚠️  连接超时           → WiFi 信号弱或不可用
❌ 连接失败，启动 AP   → 无法连接，回到配置模式
```

---

## ✅ 检查清单

首次使用前：
- [ ] 设备已刷入最新固件
- [ ] 设备电源稳定
- [ ] 手机或电脑 WiFi 已启用
- [ ] 知道家里 WiFi 的名称和密码

配置步骤中：
- [ ] 能看到 Scott_Device 热点
- [ ] 能访问 http://192.168.4.1
- [ ] 能输入 WiFi 信息
- [ ] 设备自动重启

配置完成后：
- [ ] 记下显示的设备 IP 地址
- [ ] 等待设备重启完成
- [ ] 手机连接到配置的 WiFi 网络
- [ ] 能访问 http://设备IP

---

## 🎓 理解两个网络阶段

```
🔴 阶段 1：配置阶段（使用 AP 热点）
  • 设备创建热点：Scott_Device
  • 你连接热点
  • IP: 192.168.4.1（固定）
  • 用途：输入 WiFi 信息
  • 时间：1-2 分钟
  • 结果：设备获得 WiFi 密码

🟢 阶段 2：运行阶段（使用 WiFi 网络）
  • 设备连接你的 WiFi
  • 你也连接同一个 WiFi
  • IP: 192.168.x.x（动态，来自路由器）
  • 用途：日常管理和配置
  • 时间：长期
  • 特点：可无线升级固件（OTA）
```

**不能混淆这两个阶段！**

---

## 💡 使用技巧

### 技巧 1：快速找到设备 IP
```
1. 打开路由器管理页面（通常 192.168.1.1 或 192.168.0.1）
2. 查看 DHCP 客户端列表
3. 找到设备名 esp32-c3 或 Scott_Device
4. 记下对应的 IP 地址
```

### 技巧 2：为设备设置静态 IP
```
在你的路由器中：
1. 进入 DHCP 设置
2. 找到 ESP32 设备的 MAC 地址
3. 为其分配静态 IP（如 192.168.1.100）
4. 这样下次启动 IP 地址不会变化
```

### 技巧 3：书签保存访问地址
```
配置完成后：
1. 浏览器收藏夹中添加：http://192.168.1.100
2. 以后直接点击书签访问
3. 或者直接输入 IP（如果设备支持 mDNS）
```

---

## 🆘 需要帮助？

1. **查看串口日志** - 最直接的诊断方法
2. **对照故障排查表** - 快速找到解决方案
3. **重新配置** - 重置 WiFi 回到初始状态
4. **检查硬件** - 确保设备电源稳定、传感器连接正常

---

**记住这个顺序：**
```
AP 热点 192.168.4.1 → WiFi 配置 → WiFi 网络 → Web UI 管理
  (首次)              (输入密码)    (日常用)    (高级功能)
```

祝你使用愉快！ 🎉
